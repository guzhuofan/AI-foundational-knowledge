# 个人 AI 底层知识学习项目协作规则

## 项目目的

这个项目不追求复杂的 wiki 结构，核心目的是记录我的学习过程，并让 Agent 在后续讨论相关问题时，能够快速知道我当前的知识掌握状况。

`学习时间线.md` 是主 index，用来记录不同时间点我学了什么、理解到了什么程度、哪些地方还有疑惑，以及相关认知是怎么变化的。

遵循渐进式披露原则：

- `学习时间线.md` 只保留精简摘要。
- 需要深究时，再通过链接回到原笔记。

## 笔记规范

项目中每个需要归档的笔记文件都应带 YAML，至少包含：

```yaml
---
date: YYYY-MM-DD
archived: false
---
```

- `date` 表示这篇笔记对应的日期。
- `archived` 表示这篇笔记的核心内容是否已纳入 `学习时间线.md`。
- 新建笔记时，`archived` 默认设为 `false`。
- 没有 YAML 的 Markdown 文件，默认不参与归档。

## 归档规则(你的职责)

只有当用户明确说“归档”时，Agent 才执行归档。

**全局归档时，Agent 需要：**

1. 先运行 `python3 scripts/list_unarchived_notes.py`，以脚本输出结果作为本次待归档笔记列表。
2. 脚本输出结果不是归档终点，而是本次归档必须继续读取和总结的输入集合。
3. 逐篇读取脚本输出的待归档笔记内容, 按 `date` 整理这些笔记；同一天内如果有多篇笔记，需要再按学习逻辑顺序组织，例如先基础概念，再章节总结，再延伸思考。
4. 将整理后的高层摘要写入 `学习时间线.md`, 时间线按日期组织.
5. 每个日期下只记录高层信息，例如：
   - 学了什么内容
   - 已经掌握了哪些概念、方法或实现细节
   - 还存在哪些疑问、误区或待补基础
   - 对某个知识点的理解发生了什么变化
6. 每个归档到 `学习时间线.md` 的笔记摘要，都带上原笔记的相对路径，方便后续回查。
7. 只有在某篇笔记的核心内容已经被写入 `学习时间线.md` 之后，才能把该源笔记的 `archived` 改为 `true`。

> `scripts/list_unarchived_notes.py` 只是归档扫描工具，负责明确"这次该读哪些笔记"；真正的归档仍包括读取、总结、写入 `学习时间线.md` 和回写 `archived` 状态。

## 归档原子性（硬约束）

归档操作必须遵循原子性原则：**写时间线条目和翻 archived 标志必须在同一次操作中完成，不允许中间停。**

执行顺序：
1. 先写完时间线条目（确保摘要已写入 `学习时间线.md` 并保存）。
2. 紧接着，在同一个会话中，立刻翻转对应源笔记的 `archived` 标志为 `true`。
3. 两步之间不允许插入其他任务、不允许暂停、不允许"等一下再翻"。

如果时间线条目已写但标志未翻，视为**未完成归档**，下次会话必须优先补翻标志。

## 讨论约定(你的职责)

后续在这个项目里讨论问题时, 优先先看 `学习时间线.md` 建立我的进度和认知上下文, 如果需要更详细的认知理解, 再按相对路径回查原笔记。

在解释概念、推导公式、分析代码或回答学习建议时，Agent 应优先结合 `学习时间线.md` 判断我当前处于什么掌握阶段，再决定解释的深度和方式。

## 图书参考约定(你的职责)

1. 当用户和你围绕某一本书持续讨论时，优先检查该书目录下是否已经存在 `原书及参考/` 或等价的 AI 参考包。
2. 如果已经存在参考包，优先先读其中的 `01-AI讨论参考.md` 建立结构理解；必要时再回看 `99-原始解析全文.md` 补原文依据。
3. 如果用户开始讨论某一本书, 但项目里还没有这本书的参考包, 且这本书的内容并不在你的训练集中, 你应主动提醒用户: 是否要先调用 `book-absorb` 这个skill, 生成 AI 参考包, 再继续讨论.

**讨论时应尽量避免两种情况：**
- 把已经掌握的内容反复从最基础开始讲，造成重复。
- 默认我已经懂了实际上还没有真正吃透的前置知识。

## 禁止事项

- 不要在未收到“归档”指令时擅自归档。
- 不要在 `学习时间线.md` 中堆积长文、原文摘抄或过度展开的分析。
- 不要省略原笔记相对路径作为链接。
- 不要把与当前归档任务无关的笔记内容一起改写。
- 不要凭空推断我已经掌握某个知识点，除非能从时间线或原笔记中找到依据。
- 不要把没有 YAML 的 Markdown 文件临时纳入归档。
