# 个人 AI 底层知识学习项目协作规则

## 项目目的

这个项目不追求复杂的 wiki 结构，核心目的是记录我的学习过程，并让 Agent 在后续讨论相关问题时，能够快速知道我当前的知识掌握状况。

`学习时间线.md` 是主 index，用来记录不同时间点我学了什么、理解到了什么程度、哪些地方还有疑惑，以及相关认知是怎么变化的。

遵循渐进式披露原则：
- `学习时间线.md` 只保留精简摘要。
- 需要深究时，再通过链接回到原笔记。

## 笔记规范

项目中的每个笔记文件都应带 YAML，至少包含：

```yaml
---
date: YYYY-MM-DD
archived: false
---
```

- `date` 表示这篇笔记对应的日期。
- `archived` 表示这篇笔记的核心内容是否已纳入 `学习时间线.md`。
- 新建笔记时，`archived` 默认设为 `false`。
- 没有 YAML 的 Markdown 文件，默认不参与归档。

## 新建笔记模板

新建笔记时，优先使用这个最小模板：

```yaml
---
date: YYYY-MM-DD
archived: false
---
```

```md
# 标题

今天做了什么：

学到了什么：

哪些地方已经理解：

哪些地方还模糊或需要复习：

有什么新的判断或联想：
```

- 正文不必严格按这个格式写，只要信息清楚即可。
- 可以只写几句话，也可以自由展开。
- 归档时会从正文中提炼时间线摘要。

## 归档规则

只有当用户明确说“归档”时，Agent 才执行归档。

归档时，Agent 需要：
1. 先运行 `python3 scripts/list_unarchived_notes.py`，以脚本输出结果作为本次待归档笔记列表。
2. 脚本输出结果不是归档终点，而是本次归档必须继续读取和总结的输入集合。
3. 只归档文件头带 YAML，且 YAML 中 `archived: false` 的 Markdown 文件。
4. 没有 YAML 的 Markdown 文件，不纳入归档范围。
5. 逐篇读取脚本输出的待归档笔记内容，不能只看文件名就结束。
6. 按 `date` 整理这些笔记；同一天内如果有多篇笔记，需要再按学习逻辑顺序组织，例如先基础概念，再章节总结，再延伸思考。
7. 将整理后的高层摘要写入 `学习时间线.md`。
8. 时间线按日期组织；没有任务、变化或新增判断的日期，不需要写入。
9. 每个日期下只记录高层信息，例如：
   - 学了什么内容
   - 已经掌握了哪些概念、方法或实现细节
   - 还存在哪些疑问、误区或待补基础
   - 对某个知识点的理解发生了什么变化
10. 不复制大段原文，不把 `学习时间线.md` 写成第二份笔记。
11. 每次归档都带上原笔记的 Markdown 链接，方便后续回查。
12. 只有在某篇笔记的核心内容已经被写入 `学习时间线.md` 之后，才能把该源笔记的 `archived` 改为 `true`。

`archived: true` 的含义固定为：这篇笔记的核心内容已经被纳入 `学习时间线.md`。

`scripts/list_unarchived_notes.py` 只是归档扫描工具，负责明确“这次该读哪些笔记”；真正的归档仍包括读取、总结、写入 `学习时间线.md` 和回写 `archived` 状态。

## 使用约定

后续在这个项目里讨论问题时，优先先看 `学习时间线.md` 建立时间上下文，再按链接回查原笔记。

在解释概念、推导公式、分析代码或回答学习建议时，Agent 应优先结合 `学习时间线.md` 判断我当前处于什么掌握阶段，再决定解释的深度和方式。

如果某篇笔记后来新增了还没进入时间线的重要内容，可以再把它改回 `archived: false`，等待下一次归档。

需要查看当前有哪些笔记还没归档时，优先运行 `python3 scripts/list_unarchived_notes.py`，不要靠临时猜测或手工漏查。

Agent 需要特别关注这些信息：
- 我已经掌握了哪些基础概念。
- 我对哪些知识点只是“看过”，但还没有真正理解。
- 我最近正在攻克哪些问题。
- 我有哪些反复卡住、需要多次回顾的内容。

讨论时应尽量避免两种情况：
- 把已经掌握的内容反复从最基础开始讲，造成重复。
- 默认我已经懂了实际上还没有真正吃透的前置知识。

## 禁止事项

- 不要在未收到“归档”指令时擅自归档。
- 不要在 `学习时间线.md` 中堆积长文、原文摘抄或过度展开的分析。
- 不要省略原笔记链接。
- 不要把与当前归档任务无关的笔记内容一起改写。
- 不要凭空推断我已经掌握某个知识点，除非能从时间线或原笔记中找到依据。
- 不要把没有 YAML 的 Markdown 文件临时纳入归档。
